{
  "name": "@nigerconnect/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright e2e tests for NigerConnect auth (API contract + session lifecycle + web pages)",
  "scripts": {
    "test": "playwright test",
    "test:e2e:playwright": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0"
  }
}
