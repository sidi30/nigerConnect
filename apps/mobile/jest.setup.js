/* eslint-env jest */
// react-native-gesture-handler ships its own jest setup (mocks the native module
// and exposes the RNGH jest utils used to fire gestures in tests).
require('react-native-gesture-handler/jestSetup');
