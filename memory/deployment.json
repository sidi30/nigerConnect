{
  "deployed_at": "2026-07-03T05:55:00Z",
  "git_sha": "1c9e9b4",
  "commit_message": "feat(marketplace): surface Entraide — intent-first, galerie multi-images, edit/delete",
  "status": "DEPLOYED",
  "sprint_scope": "S4 E-MARKET (surface Entraide) — GO proprio explicite 2026-07-03 (\"GO deploy and build\")",
  "server": "root@46.224.193.109",
  "app_dir": "/opt/apps/nigerConnect",
  "api_domain": "api.nigerconnect.app",
  "web_domain": "nigerconnect.app",
  "method": "git archive HEAD | ssh -> scripts/deploy-vps.sh",
  "backup_before": "backups/pre-deploy-1c9e9b4-20260703-055359.sql.gz (60K, pg_dump valide) — pris manuellement (deploy-vps.sh ne backup pas)",
  "migration": {
    "status": "ok",
    "applied": ["20260702000000_add_service_media_intent"],
    "detail": "Additif reversible: enum ServiceIntent + colonne service_requests.intent (default help_free), table service_media (cascade FK -> service_requests). Verifie live: table + index + FK CASCADE + enum {help_free,paid_service} presents."
  },
  "health_checks": {
    "internal_api": "{\"status\":\"ok\",\"checks\":{\"db\":\"ok\",\"redis\":\"ok\"}} (uptime 17s post-recreate)",
    "containers": "api + web recreated & started; postgres/redis/minio healthy inchanges",
    "post_deploy_error_grep": "aucune ligne error/fatal/exception dans les 120 dernieres lignes de logs api"
  },
  "security": {
    "pentest": "OK_TO_DEPLOY (0 critical/high/medium/low) — memory/security.json. IDOR media/ownership, Zod, cascade DELETE, privacy AUTHOR_SELECT neutralises.",
    "tests": "jest 332/332 (marketplace 15/15 dont 2 regression securite), api tsc + mobile tsc verts"
  },
  "mobile_build": {
    "delivery": "BUILD EAS (pas OTA) — gros changement, bump 1.8.0 -> 1.9.0",
    "command": "eas build --platform all --profile production --auto-submit --non-interactive --no-wait",
    "version": "1.9.0",
    "android": { "buildId": "c0512a90-5334-4299-94b4-6cfc6f07f31e", "versionCode": 11, "submit": "Play track internal", "submissionId": "60642fe8-47b5-4b98-97e6-5286ce8d49d6" },
    "ios": { "buildId": "c9e3709f-022a-4005-8e52-f762352abdda", "buildNumber": 29, "submit": "TestFlight (ASC key D7QVR3G93J)", "submissionId": "82a7eb4e-6ac8-4432-a600-16186fe1e29b" },
    "note": "Cloud EAS (--no-wait): suivre expo.dev/accounts/sidi30/projects/nigerconnect, ne pas poller. iOS 1er submit de version -> repondre Export Compliance dans ASC (chiffrement standard = Non)."
  },
  "rollback_command": "git archive e8ce4a5 | ssh -o BatchMode=yes root@46.224.193.109 'cd /opt/apps/nigerConnect && tar xf - && find . -name \"*.sh\" -exec sed -i \"s/\\r$//\" {} + && chmod +x scripts/*.sh && ./scripts/deploy-vps.sh'",
  "rollback_db": "gunzip -c backups/pre-deploy-1c9e9b4-20260703-055359.sql.gz | docker exec -i nigerconnect-postgres sh -c 'psql -U $POSTGRES_USER -d $POSTGRES_DB'",
  "rollback_note": "Migration additive (add column default + add table) — rollback code seul (e8ce4a5) laisse la table/enum en place sans casser l'ancien code (colonnes ignorees). Restaurer la DB uniquement si necessaire.",
  "notes": "S4 E-MARKET deploye API + build mobile 1.9.0 soumis. git push origin NON effectue (non demande). Deploy nominal, health verte, 0 erreur."
}
